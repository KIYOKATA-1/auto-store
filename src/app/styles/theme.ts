import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f5f9fc',
      paper: '#ffffff',
    },
    primary: {
      main: '#1976d2',
    },
  },
});

export default theme;
